FORLIB = forsub/*.o
FORSUB = forsub

#FC = ifort 
#FFLAGS =  -r8 -w -O3 
# compilation options for  linuxrtl
FC = gfortran
FFLAGS = -g -check bounds

.F.o :
	$(FC) $(FFLAGS) -c $*.F $(LIBFN) $(LIBRTL) $(LDFLAGS)
.SUFFIXES : .o .F
